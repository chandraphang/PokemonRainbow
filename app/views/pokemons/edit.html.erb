<div class="container">
<h1>Editing Pokemon</h1>
<h3><%= @pokemon.name %></h3>
<h4><%=Pokedex.find(@pokemon.pokedex_id).name%></h4>
<%= render 'form_edit', pokemon: @pokemon %>

<%= link_to 'Show', @pokemon %> |
<%= link_to 'Back', pokemons_path %>
</div>